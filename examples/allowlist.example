# Example allowlist configuration for UMRD
# 
# Format: one cgroup path per line
# Each line can optionally contain reclaim parameters after the path
#
# Example:
#   /sys/fs/cgroup/kubepods/burstable
#   /sys/fs/cgroup/kubepods/besteffort
#   /sys/fs/cgroup/kubepods/guaranteed
#
# With custom parameters:
#   /sys/fs/cgroup/your-cgroup interval_anon=5 ratio_anon=0.002 interval_file=10 ratio_file=0.0002

# Default configuration for Kubernetes pods
/sys/fs/cgroup/kubepods

# For EMM mode with custom parameters:
# /sys/fs/cgroup/kubepods interval_anon=5 ratio_anon=0.002 interval_file=10 ratio_file=0.0002
